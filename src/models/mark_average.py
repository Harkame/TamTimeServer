class MarkAverage(object):
    def __init__(self, p_stop_id, p_mark_average, p_count_marks):
        self.stop_id = p_stop_id
        self.mark_average = p_mark_average
        self.count_marks = p_count_marks
